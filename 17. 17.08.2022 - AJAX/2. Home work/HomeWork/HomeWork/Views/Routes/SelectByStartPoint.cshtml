@model IEnumerable<HomeWork.Models.RouteViewModel>


<section class="mx-5 my-4 bg-light shadow-sm border rounded-3 h-75 p-3">

    <h5 class="text-center">@ViewBag.Title</h5>

    <form method="post" asp-controller="Routes" asp-action="SelectByStartPoint" class="d-inline-flex align-items-center">
        <label for="startPoint" class="form-label">Начальный пункт:</label>
        <select id="startPoint" name="startPoint" asp-items="@ViewBag.Points" class="form-select w-16rem mx-3"></select>

        <input class="btn btn-success w-10rem me-3" type="submit" value="Выбрать">
    </form>

    @* Рендер таблицы *@
    @await Html.PartialAsync("_RoutesTableView", Model)

</section>
