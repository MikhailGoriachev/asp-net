@*
    Запрос 5.
    Выбирает информацию о зафиксированных фактах продажи для заданного
    параметром продавца
*@
@model IQueryable<WebApiDatabase.Models.Sale>

@{
    ViewBag.Title = ViewBag.Interest == null
        ? "Выбирает информацию о продажах заданного продавца"
        : $"Выбирана информация о продажах продавца {ViewBag.SellerFullName}";

}

<h4 class="text-center">@ViewBag.Title</h4>
@* Вывод информации о продажах выбранного продавца *@
<div class="row my-4">
    <form class="d-inline-block ms-3 me-3" method="post">

        <label class="form-label me-1" for="interest">Продавец: </label>
        <input class="form-select-sm me-3" id="interest" name="sellerId" type="number" />

        <button class="btn btn-sm btn-outline-info ms-1 align-baseline" type="submit">Выбрать</button>
    </form>
</div>

@await Html.PartialAsync("_SellesTable", Model)
